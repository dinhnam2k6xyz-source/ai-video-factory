[phases.setup]
aptPkgs = ["ffmpeg", "git", "curl", "libgomp1"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt || pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
